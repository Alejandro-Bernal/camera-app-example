import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'us.code.moosecodes.camera',
  appName: 'camera example app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
